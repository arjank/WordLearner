{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Phrase list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Infirstlanguage</th>
                <th>Insecondlanguage</th>
                <th>Remark</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('phrase_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.inFirstLanguage }}</td>
                <td>{{ entity.inSecondLanguage }}</td>
                <td>{{ entity.remark }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('phrase_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('phrase_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('phrase_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
